# DECISIONS

このプロジェクトで**確定した判断**の記録。採用した案だけでなく、**採用しなかった案とその理由**を必ず残す。

## 使い方

- 新しいエントリは末尾に追記する（過去のエントリは書き換えない）
- 番号は `DEC-001` から連番

---

## DEC-001: 読み=xoxp（ユーザートークン）／書き=xoxb（botトークン）の2本立てを採用

- **日付**: 2026-08-31
- **Issue**: #1
- **決定内容**: Slackとの通信は、読み取り（`users.conversations` / `conversations.history`）を本人のユーザートークン（xoxp）で、投稿（`chat.postMessage`）をbotトークン（xoxb）で行う2本立て構成にする
- **決定理由**:
  - 「読みは広く、書きは狭く」の安全原則を、トークンの種類レベルで物理的に分離できる
  - xoxpなら本人が参加している全チャンネルを横断的に読める（botを全チャンネルに招待する必要がない）
  - xoxbは招待されたチャンネルにしか投稿できないため、投稿範囲を「botを招待したチャンネル」＋アプリ側の`replyAllowlist`の二重で制限できる
- **検討した代替案**:
  - botトークンのみで統一 → botを全チャンネルに招待する必要があり、運用上手間かつ「知らないチャンネルにも勝手に投稿できる状態」を作りやすいため不採用
  - Slack Events API（リアルタイムWebhook）でメンション検知 → 常駐サーバーが必要になりGitHub Actions定期実行の設計方針（おざけん式）と合わないため不採用（Phase 2以降で即時リレーを検討する余地は残す）
- **再検討条件**: Phase 2でリアルタイム性が必須要件になった場合、Events API＋常駐サーバー構成を再検討する
- **状態**: 有効

---

## DEC-002: 生ログはコミットしない・週報は要約のみ（Phase 2）

- **日付**: 2026-08-31
- **Issue**: #1
- **決定内容**: `state/last_seen.json` にはチャンネルごとの最終処理タイムスタンプのみを保持し、メッセージ本文・スレッド内容などの生の会話ログはリポジトリに一切コミットしない。週報等の蓄積機能はPhase 2で「要約のみ」を対象とする
- **決定理由**:
  - Slackの会話には顧客の個人情報・機密情報が含まれうる。GitHubリポジトリ（プライベートであっても）に生ログを永続化するのは情報漏えいリスクが高い
  - Phase 1のstate管理の目的は「重複応答の防止」のみであり、タイムスタンプだけで十分に達成できる
- **検討した代替案**:
  - 処理したメッセージ本文もstateに残す（デバッグ・監査のため） → 上記リスクを理由に不採用。デバッグはActionsのログ（一定期間で自動削除される）で代替する
- **再検討条件**: Phase 2で週報・要約機能を実装する際、要約テキスト（生ログではない）をどこにどう保存するかは別途設計する
- **状態**: 有効

---

## DEC-003: エリアとして新設（既存 slack-ai-agent には足さない）

- **日付**: 2026-08-31
- **Issue**: #1
- **決定内容**: 「けいたろう秘書」は `001Area/slack-ai-agent`（既存のSlack botの土台）に機能追加するのではなく、`001Area/slack-secretary` として別エリア・別リポジトリで新設する
- **決定理由**:
  - `slack-ai-agent` は常駐サーバー型のSlack bot基盤であり、思想・実行モデルが異なる（常駐プロセス＋Events API前提）
  - 「けいたろう秘書」はGitHub Actionsの定期実行（おざけん式）＋徘徊読み取りという全く別の実行モデルを採用しており、既存基盤に無理に載せると両者の設計思想が混ざり保守性が落ちる
  - 参考: MEMORY「Slack botの土台はslack-ai-agent（新規に作らずスキルを足す）」の原則はあるが、実行モデルそのものが異なる今回は例外として新設が妥当と判断
- **検討した代替案**:
  - `slack-ai-agent` にスキルとして追加 → 常駐プロセス前提の設計に定期実行バッチを混在させると、依存関係・デプロイ方式・権限設計が複雑化するため不採用
- **再検討条件**: Phase 2以降で `slack-ai-agent` 側にも定期実行の仕組みが整い、統合した方がむしろシンプルになると判断できた場合
- **状態**: 有効
