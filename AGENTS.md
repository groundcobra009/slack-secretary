# AGENTS.md — このリポジトリでのAIエージェント運用ルール

このリポジトリは tanagokokoro.AI の案件/エリアディレクトリ（指揮・記録は ai-company リポジトリ側）。
Codex・Claude 等のAIエージェントは以下を守ること。

## 書き込み範囲・削除禁止

- 書き込みは**このリポジトリ内のみ**。他のディレクトリ（隣接リポジトリ・ホーム直下等）に書かない
- **ファイル・ディレクトリの削除禁止**。`rm` を使わない。不要ファイルは `_archive/` へ移動する
- シークレット（APIキー・パスワード・`.env` の中身）を表示・出力しない。Gitにもコミットしない

## 外部に影響する行動の禁止

- メール送信・SNS投稿の公開・請求書/見積書の発行・本番デプロイ・課金が発生する操作は**絶対にしない**（下書き作成まで）
- 公開・第三者共有が必要な成果物を作ったら、その旨を最終メッセージで報告する（承認は ai-company の承認キュー経由で社長が行う）

## Codex固有

- **`gh` コマンドを使わない**（Issue・PR操作は Claude 側が行う。必要な情報はファイルで渡される）
- 非対話実行（`codex exec`）では、渡された引き継ぎ書・プロンプトに従い、不明点は推測で進めず停止して最終メッセージで報告する

## 公開名義

公開成果物（スライド・note・SNS等の外に出るもの）の名義は「**けいたろう**」。本名を公開成果物に出力しない。

## 記録

- 確定した変更は `CHANGELOG.md` に新しい順で記録（無ければ作成）
- 設計・技術の確定判断は `DECISIONS.md` に追記（過去エントリは書き換えない）
